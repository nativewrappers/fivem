[@nativewrappers/fivem](../../../../README.md) / [redm/definitions/redm](../README.md) / RegisterStreamingFileFromUrl

# Function: RegisterStreamingFileFromUrl()

## Call Signature

```ts
function RegisterStreamingFileFromUrl(registerAs, url): void;
```

Defined in: [src/common-game/definitions/redm.d.ts:20838](https://github.com/nativewrappers/nativewrappers/blob/1018756065597ab2c523914c09f2c0847f99cfd4/src/common-game/definitions/redm.d.ts#L20838)

**Experimental**: This native may be altered or removed in future versions of CitizenFX without warning.
Registers a file from an URL as a streaming asset in the GTA streaming subsystem. This will asynchronously register the asset, and caching is done based on the URL itself - cache headers are ignored.
Use `IS_STREAMING_FILE_READY` to check if the asset has been registered successfully.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `registerAs` | `string` | The file name to register as, for example `asset.ydr`. |
| `url` | `string` | The URL to fetch the asset from. |

### Returns

`void`

## Call Signature

```ts
function RegisterStreamingFileFromUrl(registerAs, url): void;
```

Defined in: [src/common-game/definitions/redm.d.ts:20850](https://github.com/nativewrappers/nativewrappers/blob/1018756065597ab2c523914c09f2c0847f99cfd4/src/common-game/definitions/redm.d.ts#L20850)

**Experimental**: This native may be altered or removed in future versions of CitizenFX without warning.
Registers a file from an URL as a streaming asset in the GTA streaming subsystem. This will asynchronously register the asset, and caching is done based on the URL itself - cache headers are ignored.
Use `IS_STREAMING_FILE_READY` to check if the asset has been registered successfully.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `registerAs` | `string` | The file name to register as, for example `asset.ydr`. |
| `url` | `string` | The URL to fetch the asset from. |

### Returns

`void`

## Call Signature

```ts
function RegisterStreamingFileFromUrl(registerAs, url): void;
```

Defined in: [src/redm/definitions/redm.d.ts:20838](https://github.com/nativewrappers/nativewrappers/blob/1018756065597ab2c523914c09f2c0847f99cfd4/src/redm/definitions/redm.d.ts#L20838)

**Experimental**: This native may be altered or removed in future versions of CitizenFX without warning.
Registers a file from an URL as a streaming asset in the GTA streaming subsystem. This will asynchronously register the asset, and caching is done based on the URL itself - cache headers are ignored.
Use `IS_STREAMING_FILE_READY` to check if the asset has been registered successfully.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `registerAs` | `string` | The file name to register as, for example `asset.ydr`. |
| `url` | `string` | The URL to fetch the asset from. |

### Returns

`void`

## Call Signature

```ts
function RegisterStreamingFileFromUrl(registerAs, url): void;
```

Defined in: [src/redm/definitions/redm.d.ts:20850](https://github.com/nativewrappers/nativewrappers/blob/1018756065597ab2c523914c09f2c0847f99cfd4/src/redm/definitions/redm.d.ts#L20850)

**Experimental**: This native may be altered or removed in future versions of CitizenFX without warning.
Registers a file from an URL as a streaming asset in the GTA streaming subsystem. This will asynchronously register the asset, and caching is done based on the URL itself - cache headers are ignored.
Use `IS_STREAMING_FILE_READY` to check if the asset has been registered successfully.

### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `registerAs` | `string` | The file name to register as, for example `asset.ydr`. |
| `url` | `string` | The URL to fetch the asset from. |

### Returns

`void`
