export enum KnockOffVehicle {
    Default,
    Never,
    Easy,
    Hard,
}

export enum TamingState {
    Invalid,
    Inactive,
    TargetDetected,
    CalledOut,
    Mountable,
    BeingPatted,
    BreakingActive,
    Spooked,
    Retreating,
    Fleeing,
}

export enum eDamageCleanliness {
    Poor,
    Good,
    Perfect,
}
